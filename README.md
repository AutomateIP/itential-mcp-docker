## Clone the Itential MCP Repo

`git clone https://github.com/itential/itential-mcp`

## Copy the .env.example into .env and populate the fields

## Bring the Compose Up

`docker compose up -d`

### Notes
- This setup assumes docker and compose plugin are installed
